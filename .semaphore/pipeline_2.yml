version: v1.0
name: Heroku deployment
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      env_vars:
        - name: APP_ENV
          value: local
      jobs:
        - name: Push code
          commands:
            - checkout --use-cache
            - git config --global user.email "wladik11399@gmail.com"
            - git config --global user.name "wladik06"
            - 'git remote add heroku https://git.heroku.com/fathomless-lowlands-91208.git'
            - git push heroku master
